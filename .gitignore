.vault-password
vars/jenkins-local.yml
vars/github-local.yml
# Legacy single-vault file; no longer loaded, but keep ignored during migration.
vars/jenkins-local-vault.yml
vars/jenkins-local-vault.local.yml
vars/jenkins-local-vault.staging.yml
vars/jenkins-local-vault.production.yml
.venv/
*.retry
inventories/*/vault.yml
inventories/*/vault.yaml
inventories/*/vault.yaml
.venv
.vscode/
.vault-password*

# Semaphore UI local configuration and secrets
semaphore/.env
semaphore/secrets/*
!semaphore/secrets/.gitkeep
semaphore/backups/*
!semaphore/backups/.gitkeep
