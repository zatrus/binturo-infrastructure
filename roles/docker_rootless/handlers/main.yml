---
- name: Restart rootless Docker
  become: true
  become_user: "{{ binturo_user }}"
  ansible.builtin.systemd:
    name: docker.service
    scope: user
    state: restarted
    daemon_reload: true
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ binturo_uid }}"
    DBUS_SESSION_BUS_ADDRESS: "unix:path=/run/user/{{ binturo_uid }}/bus"
  when:
    - binturo_docker_available | default(false)
    - binturo_dockerd_available | default(false)
    - rootless_docker_service.stat.exists | default(false)
