{{ organizers_application_log_file }} {
    hourly
    rotate -1
    missingok
    notifempty
    copytruncate
    dateext
    dateformat -%Y%m%d-%H%M%S
    olddir {{ organizers_log_directory }}/archive
    su {{ binturo_user }} {{ binturo_group }}
}
