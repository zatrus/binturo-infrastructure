---
- name: Configure Binturo application host
  hosts: binturo_hosts
  become: true
  roles:
    - common
    - app_user
    - docker_rootless
    - directory_layout
    - postgres
    - caddy
    - monitoring

