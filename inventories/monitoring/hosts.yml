---
all:
  children:
    central_monitoring:
      hosts:
        binturo-monitoring:
          # Set the private LAN or VPN address before running the playbook.
          ansible_host: 10.0.0.2
          ansible_user: binturo
